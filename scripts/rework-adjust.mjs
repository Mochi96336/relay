import { readFileSync, writeFileSync } from 'node:fs';

function replaceExact(text, from, to, expected, label) {
  const count = text.split(from).length - 1;
  if (count !== expected) throw new Error(`${label}: expected ${expected} matches, found ${count}`);
  return text.replace(from, to);
}

let html = readFileSync('public/index.html', 'utf8');
html = replaceExact(
  html,
  `    <link rel="stylesheet" href="/live-state.css" />\n    <link rel="stylesheet" href="/song-surface.css" />`,
  `    <link rel="stylesheet" href="/live-state.css" />\n    <link rel="stylesheet" href="/adjust.css" />\n    <link rel="stylesheet" href="/song-surface.css" />`,
  1,
  'Adjust stylesheet link',
);

const oldAdjust = `        <details class="adjust-panel">\n          <summary>Adjust</summary>\n          <div class="adjust-sheet">\n            <div class="adjust-heading">\n              <strong>Adjust</strong>\n              <span>Room mix + this phone</span>\n            </div>\n\n            <label class="mix-row" for="mic-gain">\n              <span>Voice</span>\n              <input id="mic-gain" type="range" min="0" max="36" step="1" value="24" disabled />\n              <output id="mic-gain-value" for="mic-gain">+24 dB</output>\n            </label>\n            <p id="mic-gain-advice" class="microcopy">Voice guidance appears while singing.</p>\n\n            <label class="mix-row" for="song-level">\n              <span>Song</span>\n              <input id="song-level" type="range" min="0" max="100" step="1" value="40" disabled />\n              <output id="song-level-value" for="song-level">40%</output>\n            </label>\n\n            <label class="mix-row" for="listen-gain">\n              <span>Monitor</span>\n              <input id="listen-gain" type="range" min="0" max="100" step="1" value="70" />\n              <output id="listen-gain-value" for="listen-gain">70%</output>\n            </label>\n\n            <div class="timing-row">\n              <div>\n                <span class="section-label">Timing</span>\n                <strong id="calibrate-status">Waiting for playback</strong>\n              </div>\n              <button id="calibrate-timing" class="text-action" type="button">Recalibrate</button>\n            </div>\n          </div>\n        </details>`;

const newAdjust = `        <details class="adjust-panel">\n          <summary>Adjust</summary>\n          <div class="adjust-sheet">\n            <div class="adjust-heading">\n              <strong>Adjust</strong>\n              <span>Performance tuning</span>\n            </div>\n\n            <section class="adjust-group room-mix" aria-labelledby="room-mix-label">\n              <div class="adjust-group-heading">\n                <span id="room-mix-label" class="section-label">Room mix</span>\n                <span>Shared</span>\n              </div>\n\n              <div class="adjust-control voice-adjust">\n                <div class="adjust-row-heading">\n                  <strong>Voice</strong>\n                  <output id="mic-gain-value" for="mic-gain">+24 dB</output>\n                </div>\n\n                <div class="voice-input-evidence">\n                  <div class="evidence-heading">\n                    <span>Input</span>\n                    <output id="mic-input-value">Listening…</output>\n                  </div>\n                  <div id="mic-input-meter" class="voice-input-meter" aria-hidden="true"></div>\n                </div>\n\n                <div class="gain-control">\n                  <div class="gain-rail">\n                    <input id="mic-gain" class="adjust-range" type="range" min="0" max="36" step="1" value="24" disabled aria-label="Voice gain" />\n                    <span id="mic-gain-recommendation-marker" class="recommendation-marker" hidden aria-hidden="true"></span>\n                  </div>\n                  <div class="recommendation-meta">\n                    <span id="mic-gain-recommendation">Sing normally for a moment.</span>\n                    <button id="use-mic-gain-suggestion" class="text-action recommendation-action" type="button" hidden>Use suggestion</button>\n                  </div>\n                  <p id="mic-gain-advice" class="adjust-helper">Relay will suggest a stable gain after it has enough voice.</p>\n                </div>\n              </div>\n\n              <label class="adjust-control" for="song-level">\n                <div class="adjust-row-heading">\n                  <strong>Song</strong>\n                  <output id="song-level-value" for="song-level">40%</output>\n                </div>\n                <input id="song-level" class="adjust-range" type="range" min="0" max="100" step="1" value="40" disabled />\n              </label>\n            </section>\n\n            <section class="adjust-group local-listen" aria-labelledby="local-listen-label">\n              <div class="adjust-group-heading">\n                <span id="local-listen-label" class="section-label">This phone</span>\n                <span>Local only</span>\n              </div>\n              <label class="adjust-control" for="listen-gain">\n                <div class="adjust-row-heading">\n                  <strong>Listen volume</strong>\n                  <output id="listen-gain-value" for="listen-gain">70%</output>\n                </div>\n                <input id="listen-gain" class="adjust-range" type="range" min="0" max="100" step="1" value="70" />\n              </label>\n              <p id="listen-adjust-state" class="adjust-helper">Listen is off. Volume is kept for next time.</p>\n            </section>\n\n            <section class="adjust-group timing-adjust" aria-labelledby="timing-adjust-label">\n              <div class="adjust-group-heading">\n                <span id="timing-adjust-label" class="section-label">Timing</span>\n                <span>Room alignment</span>\n              </div>\n              <div class="timing-row">\n                <strong id="calibrate-status">Waiting for playback</strong>\n                <button id="calibrate-timing" class="text-action" type="button">Recalibrate</button>\n              </div>\n            </section>\n          </div>\n        </details>`;
html = replaceExact(html, oldAdjust, newAdjust, 1, 'Adjust DOM');
writeFileSync('public/index.html', html);

let app = readFileSync('public/app.js', 'utf8');
app = replaceExact(
  app,
  `const micGainAdvice = document.querySelector('#mic-gain-advice');\nconst songLevel = document.querySelector('#song-level');`,
  `const micGainAdvice = document.querySelector('#mic-gain-advice');\nconst micInputMeter = document.querySelector('#mic-input-meter');\nconst micInputValue = document.querySelector('#mic-input-value');\nconst micGainRecommendation = document.querySelector('#mic-gain-recommendation');\nconst micGainRecommendationMarker = document.querySelector('#mic-gain-recommendation-marker');\nconst useMicGainSuggestion = document.querySelector('#use-mic-gain-suggestion');\nconst songLevel = document.querySelector('#song-level');`,
  1,
  'Voice Adjust selectors',
);

const oldGainAdvice = `function renderGainAdvice() {\n  if (!micGainAdvice) return;\n  const rawPeak = latestMixHealth?.micPeakDbfs;\n  const rawRecommended = latestMixHealth?.recommendedMicGainDb;\n  const peak = rawPeak === null || rawPeak === undefined ? Number.NaN : Number(rawPeak);\n  const recommended = rawRecommended === null || rawRecommended === undefined\n    ? Number.NaN\n    : Number(rawRecommended);\n\n  if (!Number.isFinite(peak) || !Number.isFinite(recommended)) {\n    micGainAdvice.textContent = '開始唱之後，這裡會顯示實際電平與建議的 Mic gain。';\n    return;\n  }\n\n  const current = Math.round(Number(micGain.value) || 0);\n  const off = recommended - current;\n  const verdict = Math.abs(off) <= 3\n    ? '目前設定合適'\n    : off < 0\n      ? \`偏高 \${-off} dB，動態會被壓平\`\n      : \`偏低 \${off} dB，人聲會太小\`;\n\n  micGainAdvice.textContent = \`峰值 \${peak.toFixed(1)} dBFS · 建議 +\${recommended} dB · \${verdict}\`;\n}`;

const newGainAdvice = `function renderGainAdvice() {\n  if (\n    !micGainAdvice || !micInputMeter || !micInputValue\n    || !micGainRecommendation || !micGainRecommendationMarker || !useMicGainSuggestion\n  ) return;\n\n  const rawPeak = latestMixHealth?.micPeakDbfs;\n  const rawRecommended = latestMixHealth?.recommendedMicGainDb;\n  const peak = rawPeak === null || rawPeak === undefined ? Number.NaN : Number(rawPeak);\n  const recommended = rawRecommended === null || rawRecommended === undefined\n    ? Number.NaN\n    : Number(rawRecommended);\n\n  if (Number.isFinite(peak)) {\n    // Evidence only: the rail shows the measured input, not another setting.\n    // -60 dBFS maps to the quiet edge and 0 dBFS to full scale.\n    const inputPercent = Math.max(0, Math.min(100, ((peak + 60) / 60) * 100));\n    micInputMeter.style.setProperty('--input-level', \`\${inputPercent}%\`);\n    micInputValue.value = \`\${peak.toFixed(1)} dBFS\`;\n  } else {\n    micInputMeter.style.setProperty('--input-level', '0%');\n    micInputValue.value = 'Listening…';\n  }\n\n  const current = Math.round(Number(micGain.value) || 0);\n  if (!Number.isFinite(recommended)) {\n    micGainRecommendationMarker.hidden = true;\n    micGainRecommendation.textContent = 'Sing normally for a moment.';\n    micGainAdvice.textContent = 'Relay will suggest a stable gain after it has enough voice.';\n    useMicGainSuggestion.hidden = true;\n    return;\n  }\n\n  const suggested = Math.max(0, Math.min(36, Math.round(recommended)));\n  const markerPercent = (suggested / 36) * 100;\n  micGainRecommendationMarker.hidden = false;\n  micGainRecommendationMarker.style.left = \`\${markerPercent}%\`;\n  micGainRecommendation.textContent = \`Recommended +\${suggested} dB\`;\n\n  const off = suggested - current;\n  micGainAdvice.textContent = Math.abs(off) <= 3\n    ? 'Sounds good'\n    : off < 0\n      ? \`\${-off} dB above suggestion\`\n      : \`\${off} dB below suggestion\`;\n\n  const canApply = publisherActive && Math.abs(off) > 3;\n  useMicGainSuggestion.hidden = !canApply;\n  useMicGainSuggestion.disabled = !publisherActive;\n  useMicGainSuggestion.textContent = \`Use +\${suggested} dB\`;\n}`;
app = replaceExact(app, oldGainAdvice, newGainAdvice, 1, 'Voice gain presentation');

app = replaceExact(
  app,
  `  micGain.disabled = !publisherActive;\n  songLevel.disabled = !publisherActive;\n  // Same dependency on publisher state, so it rides along rather than needing a`,
  `  micGain.disabled = !publisherActive;\n  songLevel.disabled = !publisherActive;\n  renderGainAdvice();\n  // Same dependency on publisher state, so it rides along rather than needing a`,
  1,
  'recommendation ownership refresh',
);

app = replaceExact(
  app,
  `for (const slider of [micGain, songLevel]) {\n  slider.addEventListener('input', () => {\n    markSliderTouched(slider);\n    sendMixSettings();\n  });\n  slider.addEventListener('change', () => markSliderTouched(slider));\n}\n\ncalibrateButton.addEventListener('click', () => {`,
  `for (const slider of [micGain, songLevel]) {\n  slider.addEventListener('input', () => {\n    markSliderTouched(slider);\n    sendMixSettings();\n  });\n  slider.addEventListener('change', () => markSliderTouched(slider));\n}\n\nuseMicGainSuggestion.addEventListener('click', () => {\n  const recommended = Number(latestMixHealth?.recommendedMicGainDb);\n  if (!publisherActive || !Number.isFinite(recommended)) return;\n  micGain.value = String(Math.max(0, Math.min(36, Math.round(recommended))));\n  markSliderTouched(micGain);\n  sendMixSettings();\n});\n\ncalibrateButton.addEventListener('click', () => {`,
  1,
  'Use suggestion action',
);
writeFileSync('public/app.js', app);

let listen = readFileSync('public/listen.js', 'utf8');
listen = replaceExact(
  listen,
  `const gainValue = document.querySelector('#listen-gain-value');\nconst note = document.querySelector('#listen-note');\n\nif (toggle && gainControl && gainValue && note) {`,
  `const gainValue = document.querySelector('#listen-gain-value');\nconst note = document.querySelector('#listen-note');\nconst adjustState = document.querySelector('#listen-adjust-state');\n\nif (toggle && gainControl && gainValue && note && adjustState) {`,
  1,
  'Listen Adjust state selector',
);
listen = replaceExact(
  listen,
  `  function render(state, copy = '') {\n    toggle.dataset.state = state;\n    toggle.setAttribute('aria-pressed', state === 'on' ? 'true' : 'false');\n    toggle.textContent = state === 'on' ? '● Listen' : 'Listen';\n    note.textContent = copy;\n  }`,
  `  function render(state, copy = '') {\n    toggle.dataset.state = state;\n    toggle.setAttribute('aria-pressed', state === 'on' ? 'true' : 'false');\n    toggle.textContent = state === 'on' ? '● Listen' : 'Listen';\n    note.textContent = copy;\n    document.body.dataset.listen = state;\n    adjustState.textContent = state === 'on'\n      ? 'Playing Relay mix on this phone.'\n      : copy.includes('timing setup')\n        ? 'Listen is off for timing setup. Volume is kept for next time.'\n        : 'Listen is off. Volume is kept for next time.';\n  }`,
  1,
  'Listen local ownership copy',
);
writeFileSync('public/listen.js', listen);

let style = readFileSync('public/style.css', 'utf8');
const oldAdjustStart = style.indexOf('.adjust-sheet {');
const oldAdjustEndMarker = '#calibrate-timing { flex: 0 0 auto; color: #bdbec2; }\n';
const oldAdjustEnd = style.indexOf(oldAdjustEndMarker);
if (oldAdjustStart < 0 || oldAdjustEnd < oldAdjustStart) throw new Error('legacy Adjust CSS block not found');
if (style.indexOf('.adjust-sheet {', oldAdjustStart + 1) !== -1) throw new Error('multiple legacy Adjust CSS blocks found');
style = style.slice(0, oldAdjustStart) + style.slice(oldAdjustEnd + oldAdjustEndMarker.length);
writeFileSync('public/style.css', style);

writeFileSync('public/adjust.css', `/* Adjust is a performance-tuning layer, not a second mixer dashboard. */\n.adjust-sheet {\n  position: absolute;\n  left: -22px;\n  right: -22px;\n  bottom: calc(100% + 1px);\n  max-height: min(72svh, 660px);\n  overflow: auto;\n  padding: 22px 22px 26px;\n  border: 0;\n  border-top: 1px solid #2a2c31;\n  border-radius: 0;\n  background: rgba(13, 14, 16, .985);\n  box-shadow: 0 -24px 64px rgba(0, 0, 0, .34);\n  display: grid;\n  gap: 26px;\n}\n\n.adjust-heading,\n.adjust-group-heading,\n.adjust-row-heading,\n.evidence-heading,\n.recommendation-meta,\n.timing-row {\n  display: flex;\n  align-items: baseline;\n  justify-content: space-between;\n  gap: 16px;\n}\n\n.adjust-heading strong {\n  font-size: 22px;\n  line-height: 1;\n  letter-spacing: -.035em;\n  font-weight: 640;\n}\n.adjust-heading > span,\n.adjust-group-heading > span:last-child {\n  color: #777a82;\n  font-size: 10px;\n}\n\n.adjust-group {\n  display: grid;\n  gap: 17px;\n}\n.adjust-group + .adjust-group {\n  padding-top: 22px;\n  border-top: 1px solid #25272c;\n}\n.adjust-group-heading .section-label { margin: 0; }\n\n.adjust-control {\n  display: grid;\n  gap: 10px;\n  color: #d2d3d6;\n}\n.adjust-row-heading strong {\n  font-size: 14px;\n  font-weight: 610;\n}\n.adjust-row-heading output,\n.evidence-heading output {\n  color: #a6a8ae;\n  font-size: 11px;\n  font-variant-numeric: tabular-nums;\n}\n\n.voice-input-evidence {\n  display: grid;\n  gap: 7px;\n  margin-top: 2px;\n}\n.evidence-heading {\n  color: #767981;\n  font-size: 10px;\n  text-transform: uppercase;\n  letter-spacing: .08em;\n}\n.voice-input-meter {\n  --input-level: 0%;\n  position: relative;\n  height: 2px;\n  background: #303239;\n  overflow: hidden;\n}\n.voice-input-meter::before {\n  content: \"\";\n  position: absolute;\n  inset: 0 auto 0 0;\n  width: var(--input-level);\n  background: #d8d6d1;\n  transition: width 90ms linear;\n}\n\n.gain-control { display: grid; gap: 9px; }\n.gain-rail { position: relative; }\n.adjust-range {\n  appearance: none;\n  -webkit-appearance: none;\n  width: 100%;\n  height: 22px;\n  margin: 0;\n  background: transparent;\n  cursor: pointer;\n}\n.adjust-range::-webkit-slider-runnable-track {\n  height: 2px;\n  background: #3a3c42;\n}\n.adjust-range::-webkit-slider-thumb {\n  appearance: none;\n  -webkit-appearance: none;\n  width: 12px;\n  height: 12px;\n  margin-top: -5px;\n  border: 1px solid #0d0e10;\n  border-radius: 50%;\n  background: #e5e2db;\n}\n.adjust-range::-moz-range-track { height: 2px; background: #3a3c42; }\n.adjust-range::-moz-range-thumb {\n  width: 12px;\n  height: 12px;\n  border: 1px solid #0d0e10;\n  border-radius: 50%;\n  background: #e5e2db;\n}\n.adjust-range:disabled { opacity: .42; cursor: default; }\n\n.recommendation-marker {\n  position: absolute;\n  top: 4px;\n  width: 1px;\n  height: 14px;\n  background: #858991;\n  transform: translateX(-.5px);\n  pointer-events: none;\n}\n.recommendation-meta { min-height: 24px; }\n.recommendation-meta > span {\n  color: #b4b5b9;\n  font-size: 11px;\n  font-variant-numeric: tabular-nums;\n}\n.recommendation-action {\n  padding: 3px 0;\n  color: #ddd9d2;\n  font-size: 11px;\n}\n.adjust-helper {\n  margin: 0;\n  color: #74777e;\n  font-size: 10px;\n  line-height: 1.45;\n}\n\n.local-listen .adjust-row-heading strong { font-weight: 560; }\nbody[data-listen=\"off\"] .local-listen .adjust-range { opacity: .6; }\n\n.timing-row { align-items: center; }\n#calibrate-status {\n  min-width: 0;\n  color: #c9c9cc;\n  font-size: 12px;\n  font-weight: 550;\n  line-height: 1.45;\n}\n#calibrate-timing {\n  flex: 0 0 auto;\n  padding-right: 0;\n  color: #bdbec2;\n}\n\n@media (max-height: 760px) {\n  .adjust-sheet {\n    max-height: min(78svh, 610px);\n    gap: 20px;\n    padding-top: 18px;\n  }\n  .adjust-group { gap: 13px; }\n  .adjust-group + .adjust-group { padding-top: 17px; }\n}\n`);

writeFileSync('test/adjust-ui-contract.test.ts', `import assert from 'node:assert/strict';\nimport { readFileSync } from 'node:fs';\nimport test from 'node:test';\n\nconst html = readFileSync(new URL('../public/index.html', import.meta.url), 'utf8');\nconst app = readFileSync(new URL('../public/app.js', import.meta.url), 'utf8');\nconst listen = readFileSync(new URL('../public/listen.js', import.meta.url), 'utf8');\nconst css = readFileSync(new URL('../public/adjust.css', import.meta.url), 'utf8');\n\ntest('Adjust separates shared room mix from this-phone Listen and Timing', () => {\n  assert.match(html, /id=\"room-mix-label\"[^>]*>Room mix</);\n  assert.match(html, /id=\"local-listen-label\"[^>]*>This phone</);\n  assert.match(html, />Listen volume</);\n  assert.match(html, /id=\"timing-adjust-label\"[^>]*>Timing</);\n  assert.doesNotMatch(html, />Monitor<\/);\n});\n\ntest('Voice Adjust distinguishes live input evidence from the gain setting and suggestion', () => {\n  assert.match(html, /id=\"mic-input-meter\"/);\n  assert.match(html, /id=\"mic-input-value\"/);\n  assert.match(html, /id=\"mic-gain-recommendation-marker\"/);\n  assert.match(html, /id=\"use-mic-gain-suggestion\"/);\n  assert.match(app, /micPeakDbfs/);\n  assert.match(app, /recommendedMicGainDb/);\n  assert.match(app, /Use \+\$\{suggested\} dB/);\n  assert.match(app, /useMicGainSuggestion\.addEventListener\('click'/);\n});\n\ntest('Listen owns only local playback state and preserves volume while off', () => {\n  assert.match(listen, /document\.body\.dataset\.listen = state/);\n  assert.match(listen, /Playing Relay mix on this phone\./);\n  assert.match(listen, /Volume is kept for next time\./);\n});\n\ntest('Adjust uses one flat layer and thin rails instead of a card wall', () => {\n  assert.match(html, /href=\"\/adjust\.css\"/);\n  assert.match(css, /\.adjust-sheet \{[\s\S]*?border-radius: 0;/);\n  assert.match(css, /\.voice-input-meter \{[\s\S]*?height: 2px;/);\n  assert.match(css, /\.adjust-range::-webkit-slider-runnable-track \{[\s\S]*?height: 2px;/);\n});\n`);

console.log('Adjust ownership and Voice recommendation UI refactored');
