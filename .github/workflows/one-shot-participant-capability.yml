name: one-shot participant capability fix

on:
  push:
    branches:
      - agent/stabilize-room-kernel

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - run: echo one-shot-workflow-started
